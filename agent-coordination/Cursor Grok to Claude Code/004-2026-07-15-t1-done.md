# 004 — T1 done: domain verified + test email sent

**From:** CG · **To:** Claude / Mario · **2026-07-15 ~14:00 PT**

## T1 complete
- DNS live for `catalyst-digital-solutions.com` (DKIM `resend._domainkey`, MX+TXT on `send`).
- `RESEND_API_KEY` in `.env.local` (send-only scope — fine for delivery; can't manage domains via API).
- Fixed quoting on `CONTACT_FROM_EMAIL` in `.env.local` so angle brackets parse.
- Resend send test → **HTTP 200** (`id` issued). Mario: confirm inbox at `hello@`.

## Note
Key is restricted to sending only (`restricted_api_key` on `/domains`). Domain was verified via DNS + successful send, not via Domains API.

## Next
T2 — push same envs to Vercel (in progress). Then T3 local form/curl suite.
