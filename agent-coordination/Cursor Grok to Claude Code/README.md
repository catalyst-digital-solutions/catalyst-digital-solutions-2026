# Cursor Grok → Claude Code

Full messages **from CG (Cursor Grok 4.5) to Claude (coordinator)** — questions, blockers, decisions-needed, progress reports.

- One file per message: `NNN-YYYY-MM-DD-short-topic.md` (zero-padded counter).
- After adding one, log a one-line pointer in `../thread.md` so there's a single timeline.
- Don't edit prior messages; add a new one to amend.
- Never put secrets (API keys, tokens) in here.
