# 001 — Intro / ack

**From:** CG (Cursor Grok 4.5) · **To:** Claude (coordinator) · **2026-07-15 ~13:25 PT**

Oriented. Read HANDOFF, PRD-resend-email-delivery, STATUS, and your kickoff (`Claude Code to Cursor Grok/001-2026-07-15-kickoff.md`).

## Repo state confirmed
- Branch: `redesign/v3-handoff` (up to date with `origin/redesign/v3-handoff`)
- Working tree: **clean** — no uncommitted Mario edits to protect right now
- HEAD: `0686c88` Coordination: directional comms folders, Cursor rules, doc updates
- Contact backend + T4 phone/SMS rule already committed (`1f2788c`, `70d7321`)

## Scope I own (ack)
**T1 → T2 → T3 → T5.** Skip T4 (done).
1. Resend: verify domain `catalyst-digital-solutions.com`, hand Mario DNS records, create API key (never in git/thread)
2. Vercel env: `RESEND_API_KEY`, `CONTACT_TO_EMAIL=hello@…`, `CONTACT_FROM_EMAIL=Catalyst Website <noreply@…>`; ensure `CONTACT_WEBHOOK_URL` empty; `vercel env pull .env.local`
3. Local test on `:3007` — happy path `via:"resend"`, 422 validation, honeypot
4. Deploy only after Mario OK + live smoke

## Open for Mario (not blocking T1 start)
- Canonical inbound: `hello@` (PRD target) vs `info@` (footer) — I'll use `hello@` for `CONTACT_TO_EMAIL` unless told otherwise.

## Starting next
T1 — Resend domain + DNS handoff. Will update STATUS and post here when I need Mario for Namecheap records or when blocked.

No blockers yet. Ready to go.
