# 010 — v4 design port status (preview-ready)

**From:** CG (Cursor Grok)  
**To:** Claude Code  
**Date:** 2026-07-30  
**Branch:** `redesign/v4-design-port` (not merged to `main`)

## Done
- Full visual port of 2026-07-30 Claude Design export into Next.js App Router.
- All 13 routes wired: `/`, `/about`, `/services`, 7 service pages (short live slugs), `/quick-wins`, `/pricing`, `/contact`.
- Nav rebuilt from Automation page pattern (7-item dropdown, scroll backdrop, burger &lt;1040px).
- Footer rebuilt with `info@catalyst-digital-solutions.com`.
- ⊕ disclosures + FAQ expand via `Expandable`; homepage FAQ uses `<details>`.
- Contact keeps existing `/api/contact` + consent/honeypot (`ContactContent`), padding/email updated to v4.
- `npm run build` passes.
- Preview deploy attempted (see thread / STATUS for URL).

## Locked decisions honored
- Short live slugs (not long HANDOFF table paths)
- Unverified ⊕ stats shipped; internal “verify before publishing” notes stripped
- Loopio/AutoRFP numbers kept
- Email sitewide: `info@`
- Preview only — no `main` merge / prod without Mario OK

## Open for Mario
1. Visual QA at **900px** (and 1440/1040/600) before merge — design agent request.
2. Quick Wins “credit mechanic” still has design placeholders `[X]%` / `[Y]` days — do not publish as-is until Mario fills numbers.
3. Uncommitted deletes under `initial-references/` were pre-existing — left untouched.
4. Commit + preview URL confirm when Mario wants.

## Files of note
- `src/components/v4/*V4.tsx` — page bodies
- `src/components/v4/Expandable.tsx`
- `src/components/Nav.tsx`, `Footer.tsx`
- `_design-export-2026-07-30/` — design source (untracked)
